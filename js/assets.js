// ---------- image assets ----------
// Every sprite/background image used by the game, loaded once here so
// the rest of the code can just reference these ready-made Image objects.

const doveImg = new Image();
doveImg.src = 'assets/window-dove.png';

const playerSheet = new Image();
playerSheet.src = 'assets/player-sheet.png';

const mapBgImg = new Image();
mapBgImg.src = 'assets/map-background.jpg';

const churchOpenImg = new Image();
churchOpenImg.src = 'assets/church-open.png';

const churchClosedImg = new Image();
churchClosedImg.src = 'assets/church-closed.png';
