// ---------- entry point ----------
// Everything else (world, assets, audio, rendering, input) is already
// wired up by the time this file loads, so all that's left is to kick
// off the animation loop.
loop();
